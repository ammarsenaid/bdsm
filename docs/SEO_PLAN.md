# SEO Plan

- Public routes (`/`, `/ar`) are indexable.
- Private routes (`/app`, `/admin`) are noindex via page metadata.
- `robots.ts` disallows `/app` and `/admin` for crawlers.
- Keep structured metadata per locale in future iteration.
